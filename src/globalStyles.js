import { css } from "@emotion/react";

// Global styles
const globalStyles = css`
  html,
  body {
    font-size: 16px;
    line-height: 1.5;
    color: #333;
  }

  .container {
    max-width: 1280px;
    margin: 0 auto;
  }
`;

export default globalStyles;
